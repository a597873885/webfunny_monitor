// const LogInfosController = require('../../controllers/logInfos')
const { LogInfosController } = require("../../controllers/controllers.js")

module.exports = (router) => {
  // 上报日志信息
  // router.post('/upLogInfos', LogInfosController.upLogInfos);
}
